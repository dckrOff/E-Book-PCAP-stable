<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <item
        android:id="@+id/action_add_note"
        android:icon="@drawable/ic_note_add"
        android:title="@string/reading_add_note"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_share"
        android:icon="@drawable/ic_share"
        android:title="@string/reading_share"
        app:showAsAction="ifRoom" />

    <item
        android:id="@+id/action_font_settings"
        android:icon="@drawable/ic_text_format"
        android:title="@string/reading_font_settings"
        app:showAsAction="never" />

</menu> 